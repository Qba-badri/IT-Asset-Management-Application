---
description: Creates a custom React hook for data fetching with robust state management (loading, error) and strict TypeScript typing based on backend DTOs.
---

Create a custom React hook for data fetching.

Use the standard Fetch API or Axios.

Implement robust state management returning { data, isLoading, error, refetch }.

Strongly type the API response based on the expected backend DTO.

Include a generic error handling block that safely parses backend error messages for the UI.