---
description: Scaffolds a reusable, strictly-typed React UI component using Radix UI primitives and TailwindCSS, matching enterprise ITAM aesthetics.
---

Scaffold a new, reusable React UI component.

Use the specified Radix UI primitive (e.g., Dialog, Select, Dropdown).

Apply TailwindCSS classes for styling, ensuring it looks modern and fits an enterprise ITAM aesthetic (clean lines, neutral backgrounds, clear focus states).

Write strict TypeScript interfaces for the component's Props.

Ensure the component accepts standard React className overrides and uses forwardRef if necessary.