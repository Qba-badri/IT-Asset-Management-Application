---
description: Generates a responsive React dashboard widget wrapping an ApexCharts instance, complete with typed configuration and enterprise styling.
---

Create a React dashboard widget wrapping an ApexCharts instance.

Define the chart configuration options (type, toolbar, grid, tooltip) strongly typed.

Accept data and labels as TypeScript props.

Ensure the TailwindCSS wrapper is fully responsive and handles empty data states gracefully.

Style the chart colors to match a professional enterprise dashboard theme.